# Quick Task 260410-fu0: Update Dockerfile for tests and remove unused dependencies

**Date:** 2026-04-10
**Status:** Ready for execution

## Task Boundary

1. **Update Dockerfile** - Exclude test files (*.test.ts) from production image to reduce size and avoid shipping test code
2. **Remove unused dependencies** - Clean up package.json by removing packages that are not actually imported in the codebase

## Unused Dependencies to Remove

Based on code analysis:
- `graphql` - Only appears in URI strings (https://.../graphql), never imported as a library
- `graphql-yoga` - Not imported anywhere in the codebase
- `radix-ui` - Monolithic package; only individual `@radix-ui/*` packages are used
- `ws` - Not imported anywhere in the codebase  
- `rxjs` - Not imported anywhere in the codebase

## Files to Modify

1. `Dockerfile` - Add .dockerignore pattern or COPY exclusions for test files
2. `package.json` - Remove unused dependencies
3. `bun.lock` - Will auto-update after dependency removal

## Implementation Notes

- The Dockerfile copies `lib/` directory which includes 13 test files
- Use multi-stage build to exclude tests from production image
- Keep test dependencies in devDependencies (they're needed for CI/CD)
