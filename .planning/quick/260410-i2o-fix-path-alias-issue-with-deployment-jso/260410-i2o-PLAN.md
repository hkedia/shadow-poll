---
phase: quick/260410-i2o
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
autonomous: true
requirements: []
must_haves:
  truths:
    - "deployment.json is available at runtime in Docker production container"
    - "@/ path alias resolves deployment.json correctly in production"
  artifacts:
    - path: "Dockerfile"
      provides: "deployment.json copied to production image"
      action: "add COPY for deployment.json"
  key_links:
    - from: "lib/midnight/contract-service.ts"
      to: "deployment.json"
      via: "@/ import alias"
---

<objective>
Fix @/ path alias issue with deployment.json in Docker production deployment. The deployment.json file at project root is not copied to the Docker production image, causing runtime failures when lib modules try to import it.
</objective>

<context>
@Dockerfile
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy deployment.json to Docker production stage</name>
  <files>Dockerfile</files>
  <action>
    In the production stage of the Dockerfile, after the existing COPY commands, add:
    COPY deployment.json ./

    This ensures deployment.json is available at runtime for the @/ path alias resolution in lib modules.
  </action>
  <verify>
    <automated>grep -q "deployment.json" Dockerfile && echo "deployment.json COPY present"</automated>
  </verify>
  <done>Dockerfile production stage copies deployment.json from project root</done>
</task>

</tasks>

<success_criteria>
- deployment.json is copied to Docker production image alongside server.ts and lib/
- The @/ path alias resolves correctly at runtime in the container
</success_criteria>

<output>
After completion, create `.planning/quick/260410-i2o-fix-path-alias-issue-with-deployment-jso/260410-i2o-SUMMARY.md`
</output>
