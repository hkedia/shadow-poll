---
must_haves:
  truths:
    - Wallet detection should check window.midnight['1am'] synchronously first
    - If wallet not found immediately, show "not_detected" modal instantly (no 5 second delay)
    - Connect button should show "connecting" state immediately when clicked
    - Modal should appear instantly when wallet is not detected
  artifacts:
    - path: "lib/midnight/use-wallet.ts"
      provides: "Instant wallet detection with synchronous window check"
  key_links:
    - from: "use-wallet.ts"
      to: "wallet-onboarding.tsx"
      via: "status: not_detected triggers modal immediately"
---

# Plan: Fix Wallet Detection to Show Modal Instantly

## Goal
Fix the wallet detection flow so the "1am wallet required" modal appears instantly when no wallet is detected, instead of polling for 5 seconds first.

## Task 1: Modify detectWallet and connect Flow
**Files:** `lib/midnight/use-wallet.ts`

**Action:**
1. Modify `detectWallet()` to check `window.midnight?.['1am']` synchronously first:
   - If found immediately, resolve with the wallet
   - If not found, resolve with null immediately (no polling delay)
2. Update `connect()` function to:
   - Set status to "connecting" immediately when user clicks
   - Call detectWallet()
   - If null (not detected), set status to "not_detected" immediately
   - If found, proceed with connectInternal()
3. Remove the polling logic from detectWallet (or keep it minimal with fewer attempts)

**Key Code Changes:**
```typescript
function detectWallet(): Promise<unknown | null> {
  return new Promise((resolve) => {
    // Check immediately - no delay
    const wallet = (window as any)?.midnight?.["1am"];
    resolve(wallet || null);
  });
}
```

**Verify:**
- TypeScript compiles without errors
- No lint errors

**Done:**
- detectWallet checks synchronously
- connect sets connecting state immediately
- not_detected status set instantly when wallet not found

## Task 2: Test in Chrome
**Files:** (manual testing)

**Action:**
Test with 1am extension disabled:
1. Load page
2. Click "Connect Midnight Wallet" button
3. Verify button shows "Connecting..." state immediately
4. Verify "1am.xyz Wallet Required" modal appears instantly (within <100ms, not 5 seconds)
5. Dismiss modal
6. Click Connect again
7. Verify modal reappears instantly

**Verify:**
- No 5-second delay before modal appears
- Button shows connecting state while detecting
- Modal appears instantly when no wallet

**Done:**
- All test scenarios pass
- Modal appears instantly (< 1 second)
